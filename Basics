"Mike" //This is going to produce a String of my name
"Michael Kim".length // This is going to produce a count of how many characters are within the string
3+4 // This produces a simple addition result 
3*4 // This produces a simple multiplication result

//What can we use JavaScript For?

// Make websites respond to user interaction, build apps and games (e.g. blackjack), access information on the internet (e.g. find out the top trending words on Twitter by Topic, Organize and present data (e.g. automate spreadsheet work; data visualization)

confirm("Do you like balls?"); //This will ask the user a question and ask them to confirm
prompt("Where are you from?"); //This will prompt the user with a question as well as a text field to send an answer

//Obviously, there are different types of variables, strings, numbers, booleans, etc.

"I'm coding like a champ".length > 10 // Should check to see if the character value is greater than 10 and return true or false

console.log(2*5)
console.log("Hello")

//console.log() causes the computer to print out whatever it is doing

// Greater than = > | Less than = < | Less than or Equal to <= | Equal to = ===| Not Equal to = !==

If("Mike is a Boss".length>=2) {
  console.log("Wow you are a boss")
}

//This is an if statement 

if("myname".length >= 7) {
  console.log("Let's go down the first road!");
}
else{
  console.log("Random text")
}

//Simple if statement. 

if(10===10) {
  console.log("This is true!");
}
else{
  console.log("This is false!");
}

//simple if statement as well

//I'm skipping through a bunch of the stupid shit

"wonderful day".substring(3,7);

//substring pulls a selected amount of characters out of a string. The parentheses indicates a beginning and ending character to stop at 

var myAge = 18;

console.log(myAge);

